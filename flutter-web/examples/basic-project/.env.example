# Example Flutter Web Project Configuration
# This shows a minimal configuration for a basic Flutter web project

# Project identification
PROJECT_NAME=my-flutter-app
CONTAINER_NAME=my-flutter-dev

# Local development setup
WORKSPACE_MOUNT=/Users/myname/projects/my-flutter-app
USE_HOST_GCLOUD_AUTH=true

# GitHub configuration
GITHUB_TOKEN=ghp_YOUR_PERSONAL_ACCESS_TOKEN
GITHUB_USERNAME="Your Name"
GITHUB_EMAIL=you@example.com

# Optional: Only if using Firebase
FIREBASE_PROJECT_ID=my-flutter-app
GOOGLE_CLOUD_PROJECT=my-flutter-app

# Ports (defaults are usually fine)
WEB_PORT=8080

# Optional: Only if using AI assistance
ANTHROPIC_API_KEY=sk-ant-YOUR_KEY_HERE